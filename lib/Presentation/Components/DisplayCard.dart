import 'package:flutter/material.dart';

class DisplayCard extends StatelessWidget {
  final size;
  DisplayCard(this.size);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
